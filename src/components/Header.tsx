import React from "react";
import styles from "./Header.module.css";

const Header: React.FC = () => {
  return <header className={styles.header}>Concurrent Crypto Status</header>;
};

export default Header;
